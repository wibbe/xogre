
SET(SOURCE
  Scene.cpp
)

SET(LIBRARIES
  ${DEPENDENCY_LIBS}
)

ADD_LIBRARY(xogre ${SOURCE})
TARGET_LINK_LIBRARIES(xogre ${LIBRARIES})

# Installation
INSTALL(TARGETS xogre DESTINATION lib)
INSTALL(DIRECTORY "${PROJECT_SOURCE_DIR}/include/" DESTINATION include)

