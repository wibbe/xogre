
SET(SOURCE
  HandleManager.cpp
)

SET(LIBRARIES
)

ADD_LIBRARY(xogre ${SOURCE})
TARGET_LINK_LIBRARIES(xogre ${LIBRARIES})

# Installation
INSTALL(TARGETS xogre CONFIGURATIONS Release DESTINATION lib)

